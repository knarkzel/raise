[package]
name = "raise"
version = "0.1.0"
edition = "2024"

[profile.release]
strip = true

[dependencies]
argh = "0.1"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
